{
  "name": "escala-figma-plugin",
  "version": "0.2.1",
  "license": "MIT",
  "author": "Cesar Durango (Duscenko)",
  "scripts": {
    "build": "tsc --noEmit && node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.96.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
